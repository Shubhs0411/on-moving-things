# ── Optional: pre-fill a default API key on the server ──────────────────────
# Leave blank — users enter their own key in the browser.
# Only set these if you are hosting a shared instance where you want to
# supply the key yourself (useful for demos or internal tools).

# DEFAULT_PROVIDER=anthropic
# DEFAULT_API_KEY=sk-ant-...

# ── Server settings ──────────────────────────────────────────────────────────
# These values are used by docker-compose / cloud deploy only.
PORT=8000
HOST=0.0.0.0
